{
    "rules": [
        {
            "packageSettings": {
                "maxUpdateType": "minor",
                "versioning": "(\\d+)(?:\\.(\\d+))?(?:\\.(\\d+))?"
            }
        },
        {
            "matches": {
                "datasources": [
                    "nodejs"
                ]
            },
            "packageSettings": {
                "versioning": "^v(\\d+)(?:\\.(\\d+))?(?:\\.(\\d+))?$"
            }
        }
    ]
}